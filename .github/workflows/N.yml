name: runma
on: 
  workflow_dispatch:
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: one
        run: |
          sudo apt update -y
          sudo apt install git wget -y
          curl -fsSL https://tailscale.com/install.sh | sh
          sudo tailscale up
          git clone https://github.com/dockur/macos.git
          cd macos
          docker build -t dockerd .
      - name: two
        run: |
          docker run -i --rm -p 8006:8006 -p 5900:5900/tcp -p 5900:5900/udp --device=/dev/kvm --device=/dev/net/tun --cap-add NET_ADMIN --stop-timeout 99999990 dockerd

          
